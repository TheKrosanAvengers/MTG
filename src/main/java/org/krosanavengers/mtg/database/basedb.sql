-- auto-generated definition
create table CARD
(
    ID           INT auto_increment
        primary key,
    MULTIVERSEID INT,
    NAME         VARCHAR,
    CMC          INT,
    COST         VARCHAR,
    IDENTITY     VARCHAR,
    POWER        VARCHAR,
    TOUGHNESS    VARCHAR,
    TEXT         VARCHAR
);

 